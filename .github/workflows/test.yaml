name: Build Conda Package

on: [ push ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step_1
        uses: cvoegele/action-check-tags-in-another-repo@v0.0.1
        with:
          github-url-other-repo: https://github.com/OxfordSKA/OSKAR
      - name: Step_2
        run: |
          echo ${{ Step_1.outputs.tags }}
